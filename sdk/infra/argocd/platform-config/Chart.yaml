apiVersion: v2
name: platform-config
description: Configuration templates for cluster addons (cert-manager issuers, ESO stores, Karpenter profiles)
type: application
version: 0.1.0
appVersion: "1.0.0"
