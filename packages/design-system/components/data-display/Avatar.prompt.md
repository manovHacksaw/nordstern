Circular avatar showing an image or initials, with a subtle border ring.

```jsx
<Avatar src="/me.png" alt="Ari" size={48} />
<Avatar initials="AK" size={40} />
```

Sizes: 24 / 32 / 40 / 48. Falls back to `initials` on a deep-violet fill when `src` is absent.
