Rounded surface container (24px radius, purple-tinted shadow). Set `interactive` for hover lift.

```jsx
<Card>
  <h3>Total balance</h3>
  <p>$12,480.22</p>
</Card>
<Card interactive onClick={open}>…</Card>
```

Props: `interactive`, `padding` (default 28), plus native div attributes.
